{{ form_start(userForm) }}
    {{ form_row(userForm.firstname, {
        'label': "First Name"
    }) }}
    {{ form_row(userForm.lastname, {
        'label': "Last Name"
    }) }}
    {{ form_row(userForm.email, {
        'label': "E-Mail"
    }) }}
    {{ form_row(userForm.language) }}
    {{ form_row(userForm.jobtitle, {
        'label': "Job Title"
    }) }}
    {{ form_row(userForm.team) }}
    {{ form_row(userForm.dateHire, {
        'label': "Date Hired"
    }) }}
    {{ form_row(userForm.dateLastpromotion, {
        'label': "Date Last Promotion"
    }) }}
    {{ form_row(userForm.supervisor, {
        'label': "Supervisor"
    }) }}
    {{ form_row(userForm.roles, {
        'label': "Access Level"
    }) }}

<!--button type="submit" class="btn btn-primary" formnovalidate><span class="fa fa-save"></span> {{ buttonCreateUpdate }}</button-->
{{ form_widget(userForm.btnCreateUpdate, {
    'label': '<span class="fa fa-archive"></span> ' ~ buttonCreateUpdate,
    'attr': {'class': 'btn btn-primary'}
})
}}

{% if (buttonArchive != 'donotshow') %}
    <br><br>
    <!--button type="submit" class="btn btn-primary" formnovalidate><span class="fa fa-archive"></span> {{ buttonArchive }}</button><br-->
    {{ form_widget(userForm.btnArchive, {
       'label': '<span class="fa fa-archive"></span> ' ~ buttonArchive,
       'attr': {'class': 'btn btn-primary'}
       })
    }}
{% endif %}

&nbsp;&nbsp;&nbsp;<a href="{{ app.request.headers.get('referer') }}">Cancel</a>

{{ form_end(userForm) }}